
let str = '     Tech       Global           ';
console.log(str.trim()); //'Tech       Global'
console.log(str.trimStart());
console.log(str.trimEnd());

let greeting = 'Hello';
console.log(greeting.trim()); //nothing happens if there is no space to remove in the beginnig and end

/*
trim()
TASK: removes the whitespaces at the beginnig and the end of the string
RETURN: returns a new string modified string with removed string
ARGUMENT: no args
CHANGE ORIGINAL VALUE: does not change the original value
STATIC or NON-STATIC: non - static

*/
